Enter **Interview Mode** for this session.

Ask the user which problem they're working on, if not already stated.

Then ask them to describe or share their design: the happy flow, the classes
derived from it, and the draw.io design with the 2-3 design patterns used.

Once you have the design, follow the Interview Mode rules in CLAUDE.md exactly:
- Do not modify the design — implement it as given, in Java.
- Ask before changing anything structural if something is ambiguous or won't compile.
- Make sure it actually runs.
- Generate test input files, run them, and validate/save output.
- Report which design patterns were implemented and where.
- Do not commit or push without explicit approval.
